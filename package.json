{
  "name": "docs-site",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build needed'",
    "serve": "npx serve ."
  },
  "engines": {
    "node": ">=20.0"
  },
  "devDependencies": {
    "typescript": "~5.6.2"
  }
}
